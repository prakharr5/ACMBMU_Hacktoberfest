#include <iostream>
#include<math.h>
using namespace std;

int main()
{
    int a;
    cout<<"enter the year to find it leap or not";
    cin>>a;
    if(a%4==0)
    cout<<("it is a leap year");
    else
    cout<<("it is not a leap year");
    return 0;
}
  
